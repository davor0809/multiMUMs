#include <string>
#include <vector>
using namespace std;

namespace sa {
    pair<vector<int>, vector<int>> calculateMUMs(string, string, vector<int>, vector<int>, bool);

    int calculateMultiMUMs(vector<string>);
}